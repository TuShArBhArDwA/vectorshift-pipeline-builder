# Base URL of the backend API.
# Local development can omit this; it defaults to http://localhost:8000.
# For a deployed frontend, set this to your deployed backend URL, e.g.:
# REACT_APP_API_URL=https://your-backend.vercel.app
REACT_APP_API_URL=http://localhost:8000
